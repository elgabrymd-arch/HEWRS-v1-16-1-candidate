Intermediate test-run screenshot, not a failed current receipt.
The test initially looked up a legacy component URL instead of the installed
content-hash path. The lookup was corrected. The final browser suite has
17 passed checks and zero failed checks. See ../browser/RESULT.json.
